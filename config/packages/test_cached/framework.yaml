framework:
    test: ~
    session:
        storage_factory_id: session.storage.factory.mock_file
    cache:
        pools:
            test.mailer_pool:
                adapter: cache.adapter.filesystem
