{% extends "templates/baseEmailTemplate.html.twig" %}

{% block subject %}
    Новый заказ №{{ order.number }}!
{% endblock %}

{% block templ0 %}
    {% set url = order.channel.hostname is not null ? 'http://' ~ order.channel.hostname ~ path('sylius_admin_order_show', {'orderId': order.id}) : url('sylius_admin_order_show', {'orderId': order.id}) %}
    {% autoescape %}
        Зарегистрирован новый заказ №{{ order.number }}.
        <br/>
        Чтобы просмотреть заказ - пожалуйста, перейдите по ссылке {{ url|raw }}
        <br/><br/>
    {% endautoescape %}
{% endblock %}